{
  "name": "untrace",
  "description": "Remove C2PA content credentials, AI generation tags and EXIF metadata from images, entirely in the browser.",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "gh-pages": "^6.1.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0",
    "lightningcss": "^1.30.1"
  },
  "dependencies": {
    "exifr": "^7.1.3"
  },
  "homepage": "https://untrace.realbrain.cc/"
}
